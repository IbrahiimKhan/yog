import { StyleSheet, Text, View } from "react-native";
import React, { ReactElement } from "react";

export const YogaScreen = (): ReactElement => {
  return (
    <View>
      <Text>YogaScreen</Text>
    </View>
  );
};

export default YogaScreen;

const styles = StyleSheet.create({});
